import { FC } from "react";
import FormTable from "./form";

const TableIndex : FC = () => {
    return (
        <div className="client">
            <FormTable />
        </div>
    );
};

export default TableIndex;